# Gas Town runtime
.gt/
.gc/
.claude/
.runtime/
embeddeddolt/
CLAUDE.md

# Nix build outputs
result
result-*

# Gas City runtime artifacts (gc init / gc start outputs)
hooks/
.beads/
/pack.toml
/city.toml
