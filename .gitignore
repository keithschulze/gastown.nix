# Nix build outputs
result
result-*

# microvm.nix runtime artifacts (created at the host CWD when starting a VM)
*.img
control.socket
*.sock

# Beads (work tracking)
.beads/*
!.beads/config.yaml
!.beads/metadata.json
